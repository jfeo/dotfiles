# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font
set $dfont Dejavu Sans Mono-10:bold
set $font pango:Dejavu Sans Mono Bold 10
font $font
# Colors 
set $bg #12171A
set $fg #F9F9F9
set $bg_sel #4C170F
set $fg_sel #F9F9F9

#colorclass             border  bg      text  indicator
client.focused          $bg_sel $bg_sel $fg   $bg
client.focused_inactive $bg     $bg     $fg   $bg
client.unfocused        $bg     $bg     $fg   $bg

#dmenu height
set $h 19

# Windows borders
new_window pixel 2
new_float  pixel 2

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -h $h -fn "$dfont" -nb "$bg" -nf "$fg" -sb "$bg_sel" -sf "$fg_sel"
bindsym $mod+z exec --no-startup-id dmenu_shutdown -h $h -fn "$dfont" -nb "$bg" -nf "$fg" -sb "$bg_sel" -sf "$fg_sel"
bindsym $mod+o exec --no-startup-id dmenu_fo.sh -i -l 10 -h $h -fn "$dfont" -nb "$bg" -nf "$fg" -sb "$bg_sel" -sf "$fg_sel"
bindsym XF86Display exec --no-startup-id drandr -h $h -fn "$dfont" -nb "$bg" -nf "$fg" -sb "$bg_sel" -sf "$fg_sel"
# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ae focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+ae move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+a layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+s layout toggle split


# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
#bindsym $mod+d focus child

# workspaces
set $ws1 1
set $ws2 2
set $ws3 3
set $ws4 4
set $ws5 5
set $ws6 6
set $ws7 7
set $ws8 8
set $ws9 9
set $ws0 10

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws0

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws0

# multimedia keys, with signal for i3blocks update
bindsym XF86AudioRaiseVolume exec "volctl vol 5%+"
bindsym XF86AudioLowerVolume exec "volctl vol 5%-"
bindsym XF86AudioMute exec "volctl toggle"


#bindsym XF86Tools exec 
bindsym XF86Search exec "catfish"
#bindsym XF86LaunchA
bindsym XF86Explorer exec "thunar"

# brightness
bindsym XF86MonBrightnessUp exec "brtctl -A 3"
bindsym XF86MonBrightnessDown exec "brtctl -U 3"

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec --no-startup-id "zenity --question --text 'Do you really want to exit i3?' && i3-msg exit"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 2 px or 2 ppt
        bindsym k resize grow height 2 px or 2 ppt
        bindsym l resize shrink height 2 px or 2 ppt
        bindsym ae resize grow width 2 px or 2 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 2 px or 2 ppt
        bindsym Down resize grow height 2 px or 2 ppt
        bindsym Up resize shrink height 2 px or 2 ppt
        bindsym Right resize grow width 2 px or 2 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        font pango: Icons, $font
        status_command i3blocks
        position top 
        colors {
                statusline $text
                background $bg
                separator  $bg

                focused_workspace  $bg $bg_sel $fg_sel
                active_workspace   $bg $bg $fg_sel
                inactive_workspace $bg $bg $fg
                urgent_workspace   $bg $bg $fg
        }
}
###############
# ASSIGNMENTS #
###############
# assign [class="URxvt"] "$ws1"
# assign [class="Chromium"] "$ws2"
# assign [class="Gimp"] "$ws3"
# assign [class="Atril"] "$ws4"
# assign [class="war3.exe"] "$ws5"
# assign [class="Steam"] "$ws5"
# assign [class="Thunar"] "$ws6"
# assign [class="Catfish"] "$ws6"
# assign [class="Spotify"] "$ws7"
# assign [class="vlc"] "$ws8"
# assign [class="Skype"] "$ws9"

############
# FLOATERS #
############
for_window [window_role="pop-up"] floating enable # browser pop-ups
for_window [title="File Operation Progress"] floating enable
for_window [title="floatgoddamnit"] floating enable

#############
# AUTOSTART #
#############

exec --no-startup-id xset -b
exec --no-startup-id feh --bg-max ~/pics/walls/pluto.png
# exec --no-startup-id compton -bcf --shadow-exclude-reg x21+0-1059 -D 5 -o 0.4 -I 0.1 -O 0.1 --shadow-exclude "_GTK_FRAME_EXTENTS@:c"
exec --no-startup-id redshift-gtk &
exec dropbox &
#exec nm-applet &
exec --no-startup-id battery_alert &
